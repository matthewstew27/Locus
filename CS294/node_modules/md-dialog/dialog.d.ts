import { EventEmitter, OnDestroy, ViewContainerRef, TemplateRef } from '@angular/core';
import { Overlay } from './overlay/overlay';
import { OverlayState } from './overlay/overlay-state';
import { TemplatePortal } from './portal/portal';
export declare class MdDialogPortal extends TemplatePortal {
    constructor(templateRef: TemplateRef<any>, viewContainerRef: ViewContainerRef);
}
export declare class MdDialog implements OnDestroy {
    private overlay;
    constructor(overlay: Overlay);
    onShow: EventEmitter<MdDialog>;
    onClose: EventEmitter<any>;
    onCancel: EventEmitter<any>;
    /** The portal to send the dialog content through */
    private portal;
    /** Is the dialog active? */
    private isOpened;
    dialogTitle: string;
    /** Overlay configuration for positioning the dialog */
    config: OverlayState;
    ngOnDestroy(): any;
    /** Show the dialog */
    show(): Promise<MdDialog>;
    /** Close the dialog */
    close(result?: any, cancel?: boolean): Promise<MdDialog>;
    private onDocumentKeypress(event);
}
export declare const MD_DIALOG_DIRECTIVES: any[];
export declare const MD_DIALOG_PROVIDERS: any[];
export declare class MdDialogModule {
}
